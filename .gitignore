# dependencies
/node_modules
/.pnp
.pnp.js

# next.js
/.next/
/out/
next-env.d.ts

# production
/build

# env
.env
.env.local
.env*.local

# cloudflare / opennext
/.open-next/
/.wrangler/
.dev.vars

# bildpipeline: erzeugte varianten und ihre buchhaltung.
# quelle sind die originale in /bilder, ausgeliefert wird das erzeugte —
# beides im repo zu halten hiesse, hundert dateien bei jeder qualitaets-
# aenderung mitzuschleppen. wird bei jedem build neu erzeugt (prebuild).
/public/b/
/public/og/
.bilder-cache.json

# misc
.DS_Store
*.pem
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.vercel
*.tsbuildinfo
